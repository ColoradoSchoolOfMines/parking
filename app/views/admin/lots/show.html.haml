%fieldset
  %legend
    = @lot.name
    %small
      (
      = @lot.type_type
      )

%p
  %b Total Spots:
  = @lot.total_spots
%p
  %b Cars in Lot:
  = @lot.cars

.form-actions
  = link_to 'Edit', edit_admin_lot_path(@lot), :class => 'btn'
  = link_to 'Cancel', admin_lots_path, :class => 'btn'
