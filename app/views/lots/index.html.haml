.mapview
  %map{name:"lot_spots"}
    - @lots.each do |lot|
      %area{shape:"poly",coords:get_coords(lot),href:lot_path(lot)}

  = image_tag "parking.png", usemap: "#lot_spots", style:"width:400px; height:400px;"